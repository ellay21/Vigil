services:
  - type: web
    name: vigil-backend
    env: node
    rootDirectory: Backend
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: GEMINI_API_KEY
        sync: false
